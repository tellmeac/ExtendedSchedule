apiVersion: v1
kind: Service
metadata:
  name: {{ include "extended-schedule.fullname" . }}
  labels:
    {{- include "extended-schedule.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "extended-schedule.selectorLabels" . | nindent 4 }}
